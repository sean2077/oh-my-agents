# build artifacts
/oma
/dist/
*.test
coverage.out

# runtime state (oma v2 ledgers and workflow state live here)
/.oma/

# OMC-era local workspace (interview state, transcript) — never tracked;
# the spec and implementation plan it once held live in docs/
/.omc/

# agent-relay cross-review ledger (process artifact, not source)
/.shared/
